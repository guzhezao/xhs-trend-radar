---
name: xhs-trend-radar
description: |
  用 MiniMax Code 内置浏览器（已登录会话）抓取小红书高赞笔记，产出一份可视化 HTML 热点报告。
  能力：关键词扩展 → 检索 → 按互动量重排 → 逐条独立 URL 验证 → 选题分层判断 → 生成带图表和可点击链接的 HTML 报告。
  触发词：小红书热点、小红书高赞、抓小红书、小红书选题、爆款拆解、对标笔记、trend radar、小红书热榜、小红书热点报告。
  即使用户只说"看看小红书上 XX 最近什么内容火"或"帮我出一份 XX 的小红书热点报告"，也应触发。
  只做"抓热点内容"这一件事。不要用于：代写内容本身、找达人/找博主/做商单选号、抖音等其他平台（本 skill 均不覆盖）。
license: MIT
metadata:
  requires:
    runtime: MiniMax Code
    tools: [browser]
---

# XHS Trend Radar · 小红书热点抓取

给内容创作者用的**小红书热点选题工具**：用你右侧那个已经登录过的浏览器，
围绕一个主题去小红书捞高赞笔记，逐条打开验证后，产出一份**可视化 HTML 报告**——
带赞藏评图表、发布新鲜度散点、驱动力分布，每条笔记都有能点开的真实链接和"先做哪个"的判断。

不导 cookie，不配 headless，不装 Playwright。

**它只做一件事：搜集小红书热点，帮你定选题。**

抓取和判断全程围绕"**笔记**"这一个对象——抓笔记、验证笔记、拆解笔记选题。
所以有个边界要守住：**这是"选题"工具，不是"选号"工具。**
如果用户要的是找达人、找博主、挑合作账号，目标对象就从"笔记"变成了"账号"，
抓笔记会退化成可有可无的手段，模型容易顺势跳过逐条验证（"识别博主不用看藏/评嘛"）。
把对象锁死在"笔记"，这个偷懒出口就没了。用户真要找达人，直接说明不在范围内即可。

（另：只覆盖小红书，不做抖音等其他平台。）

---

## 0. 前置检查（不通过就停）

**本 skill 硬依赖 MiniMax Code 的内置浏览器动作接口。执行任何抓取前，先做这一步。**

要求同时满足：

1. 当前 runtime 提供 `browser` 工具，且支持这些 action：
   `inspect` / `query` / `navigate` / `click_and_wait_for_navigation` / `back` / `scroll`
2. `inspect` 返回**不透明元素 ref**（opaque ref）与 `snapshotId`，且支持 `offset` 分页续读
3. `query` 支持 `kind: "text"`（读页面可见文本，用于查登录信号）
4. 浏览器**保留用户已有登录态**（右侧面板里的会话，不是新起的干净实例）

### 检查 A：工具能力（会话开始时做一次）

```
browser({ action: "navigate", input: { url: "https://www.xiaohongshu.com/explore" } })
browser({ action: "inspect" })
```

必须**先 navigate 再 inspect**。空白页的 `inspect` 也会返回 `snapshotId`，
单独跑 `inspect` 只能验出"工具存在"，验不出任何页面状态——那是无效自检。

判定：

- 拿到 `snapshotId` + 不透明 ref 元素句柄 → 工具可用，进检查 B
- 工具不存在 / action 名不匹配 / 只能拿 CSS selector 不能拿 opaque ref → **立即停止**，输出：

> 本 skill 需要 MiniMax Code 的内置浏览器（保留登录态 + opaque ref 快照）。
> 当前环境不满足，无法运行。请在 MiniMax Code 中执行。

### 检查 B：登录态（每次开抓前必做，不可跳过）

在检查 A 打开的 `explore` 页上，用 `query kind:"text"` 找登录信号：

| 信号 | 结论 |
|---|---|
| 页面出现「登录」按钮 / 扫码弹窗 / 搜索框 placeholder 为「登录探索更多内容」 | **未登录** |
| 能看到笔记卡片列表 + 无登录引导 | 已登录，可以开抓 |

未登录 → **停下**，输出：

> 右侧浏览器当前未登录小红书。请在右侧面板登录后告诉我，我再继续。
> （未登录状态下小红书返回 200 + 空笔记数组，不会报错，硬跑会交付空清单。）

**不要在未登录状态下开始抓取**，也不要"先试试看能不能拿到"。

### 为什么是硬依赖，不是偏好

**最关键的一条：未登录抓小红书搜索页，不会报错，会静默给你空结果。**

实测（2026-07-29，`web_fetch` 请求 `search_result?keyword=...`）：

| 观察项 | 实际返回 |
|---|---|
| HTTP 状态 | **200** |
| HTML | 完整页面，含 `__INITIAL_STATE__` |
| `search.feeds` | **`[]`** |
| `global.hasWebSession` | `false` |
| `#global` 属性 | `data-logged="0"` |
| 搜索框 placeholder | 「登录探索更多内容」 |

它**不封你、不给验证码、不返回错误码**——就是给一个结构完好但笔记数组为空的页面。

这比直接被拦更危险：一个只判断 HTTP 状态码的脚本会认为自己成功了，
然后交付一份"今天没有热点"的空清单。**静默失败是本 skill 拒绝降级的核心原因。**

其余三条依赖：

| 需求 | 本 skill 的做法 | 换成通用 headless 方案会怎样 |
|---|---|---|
| 拿到搜索结果 | 复用右侧浏览器已登录会话 | 见上表：200 + 空数组，静默失败 |
| 瀑布流里定位目标卡片 | `inspect` 快照里按可见文本比对拿 ref | CSS selector 是混淆类名（`data-v-xxxxxxx` + 构建 hash），每次发版都变 |
| 滚动后元素被卸载 | `inspect` 分页续读 + 只取未卸载区间 | selector 指向已卸载 DOM，静默拿空 |

这三条都不是"用别的工具麻烦一点"，是**流程照抄过去跑不通，而且不会告诉你跑不通**。

---

## 1. 需求确认（用 ask_user 弹选项卡，不要在对话里贴表格让用户手打）

**这一节固定用 `ask_user` 工具**，一次弹出一张多问题卡片，让用户**点选项**就能定完，
不用手动打字。**不要**把问题写成 markdown 表格贴在对话里等用户回复——那是上一版的错误做法，
用户还得自己敲字，也经常被模型漏掉。

### 调用规格（照这个结构调 ask_user）

一次 `ask_user`，`steps` 里放 5 个问题，`selectionMode` 按下表定。
**已经能从用户话里确定的项，不要再放进卡片问**（比如用户已说「近一周」，就别再问时间）。

| # | question | options（selectionMode） |
|---|---|---|
| 1 | 关键词要哪 1-3 个种子词？ | 无预设选项，让用户填（这项通常用户开口就带了，带了就别问） |
| 2 | 时间范围？ | `近 1 周` / `近 1 月` / `近 3 月（默认）`（single） |
| 3 | 要几条结果？ | `10 条（默认）` / `15 条` / `20 条（上限）`（single） |
| 4 | 排序口径？ | `按点赞（默认）` / `按收藏` / `按评论`（single） |
| 5 | 你的账号定位是什么？（用于相关性判断） | 无预设选项，让用户填；可给 `先不定，看大盘` 一个选项（single） |

- 关键词、账号定位是自由文本，卡片里让用户填（`ask_user` 的问题项可以不带 options）
- 时间/数量/排序是**枚举**，必须给预设选项让用户点，别让他打字
- **交付方式不用问**：本 skill 唯一产出就是 HTML 报告（见第 5 节）

### 行为约束（硬）

- 需求确认**只调一次 `ask_user`**，把该问的一次问完；不要一项一项分多次弹卡（那样烦）
- 用户话里已明确的项**直接采纳、不再放进卡片**（例如「最近一周 AI 工作台的 15 条」→ 时间/关键词/数量都不用问了，只补排序和定位）
- 如果 5 项用户全说清了，**跳过 ask_user**，直接进第 2 节
- **不要在卡片之外附一大段自己的"建议默认值"**：给了预设选项就够了，用户点就行。喧宾夺主的长篇分析是上一版的毛病
- 弹完卡片、拿到用户点选结果后，用一两句话复述确认（"好，近1月 / 15条 / 按收藏 / 定位是X"），再进第 2 节

如果用户上来就一句含糊的「跑个 X」，就靠这张卡片把 5 项收齐，别自作主张替他全点默认值。

**如果用户要的是"找博主 / 找达人 / 挑合作账号"**，不要弹这张卡 ——
那不在本 skill 范围内（见开头「不做」清单），直接说明并停下。

---

## 2. 关键词扩展（先做，别直接拿原词搜）

一个宽词回来的结果集中在头部大号，中小号的高互动笔记压根不出现。
把种子词扩成三组：**原词组 / 收窄组 / 平替组**。

扩展词生成后**先给用户过一眼**再执行，不要闷头搜一堆无关词。

完整方法与例子见 `references/topic-judgment.md` 第 1 节。

---

## 3. 抓取流程

### 开抓前先建数据存档（这一步在动手之前做）

**目标条数由用户在第 1 节定，你的任务是把它跑完，不是"觉得跑不完就砍"。**

上下文会随抓取变长，MiniMax Code 到一定长度会自动压缩对话。压缩会丢细节，
但**只要已完成的记录不在对话里、在磁盘上，压缩就伤不到进度**。所以开抓前先落一个文件：

`xhs-trend-<关键词>-<YYYYMMDD>.data.json` —— 进度存档 + 最终报告的数据源，一份两用。

它的结构就是报告的 `REPORT_DATA`（完整字段见 `references/report-template.md` 第 2 节），
开抓前先建好顶层骨架，`notes` 数组留空，逐条 append：

```json
{ "keyword":"…","captured_at":"…","range":"…","range_days":30,
  "sort_note":"…","account":"…","target":N,
  "lede":"","kpi":{},"drive_dist":{},"groups":{},"notes":[],"footer":{} }
```

**报告不用现在写。** 最后一步用 `scripts/build-report.py` 把这份 data.json 拼成 HTML，
图表/样式/交互都在模板里写死了，你只负责把数据填对（见第 5 节）。

然后明确回答一句：

> 本轮目标 <N> 条，逐条打开验证，每验证完一条就 append 进 data.json 的 notes，中途被压缩也能接着跑。

**跑不完不是砍 N，是接着跑。** "单轮跑不满"只有一种情况：搜索结果**真的翻到底了**
（scroll 到 atEnd 仍无新卡片），池子里就那么多。这时收尾如实报告"目标 N，完成 X"，
让用户决定要不要换词再开一轮 —— 而不是你替他把目标改小。

**以下都不是"跑不满"，是偷懒，一律不允许：**

- ❌ "剩下几条的 note_id 在瀑布流靠后，要多滚几次才找得到" —— 那就多滚几次，滚到够为止。搜索页有几百条，滚 3-5 次是正常成本。
- ❌ "已经消耗了较多 token，先出 X 条给你看效果" —— 上下文没满就不是理由；token 是用来干活的，不是省着不干的。data.json 在磁盘上，真撑不住了也能接着来，不会白费。
- ❌ "先出 X 条，剩下的问用户要不要继续" —— 用户已经说了要 N 条，不用再问。除非真到底（情况见上），否则接着抓到 N。

一句话：**只要搜索页还能滚出没抓过的新卡片，就没到停的时候。**

**如果上下文被压缩了**：读一遍 `data.json`，`notes` 里有几条就是已处理几条，
`target - notes.length` 就是还差几条，从搜索页接着抓下一条。**不要重头再来。**

**降验证率永远不允许。** 不要接了 10 条的目标，然后用"打开 1 条 + 9 条卡片摘要"来凑数——
那不是 10 条，那是 1 条。

### 单条闭环：抓一条，落一条

**不要先把 10 条卡片列成清单再回头逐条打开。** 那样做的结果是：
清单看起来已经成型了，打开详情页就退化成"可选的加料环节"，然后被跳过。

正确流程是**串行流水**——清单是一条一条累积出来的，不是先列好再填。

**强约束循环（这条不是建议，是顺序闸）：**

对每条要抓的笔记，**必须**按下面 4 步依次执行，**前一步的产物没落盘，不许发起下一步的工具调用**：

```
LOOP（每条笔记执行一次，直到 notes.length == target）：

[1] inspect 一次搜索页快照
    → 本次只允许从快照里挑【1 条】未处理过的笔记
      （已处理 = note_id 已在 data.json 的 notes 数组里）
    → 本次只允许抄下 3 个值：标题文本、note_id、xsec_token
      （全部从卡片 href 里原样取，形如 /search_result/<note_id>?xsec_token=...&xsec_source=）
    → 抄第 2 条就是违规
    → 不许抄点赞数、不许抄作者、不许整理"本批候选"列表

[2] 把这一条**先 append 进 data.json 的 notes 数组**（用 edit/write 落盘，不是只在对话里说）
    → 追加一条 {"note_id","title","url":"<拼好的带token链接>","status":"pending"}
    → 落盘之前，**禁止调用 click_and_wait_for_navigation**
    → 这一步的作用：把"我接下来要打开的就是它"写进磁盘，压缩也冲不掉

[3] click_and_wait_for_navigation 进详情页
    inspect 详情页快照
    → 拿 赞/藏/评 + 标签 + 发布时间；正文只扫一眼判"门槛"（可直接做/需素材），不整段抄进 data.json
    → 回 data.json **更新刚才那条**：补 like/save/comment/tags/publish_date/days_ago + judge 四项，status 改 "verified"
    → 拿不到 token 或详情页打不开 → 这条 status 改 "failed"，补 fail_reason 写明原因
    → 失败记录也算"这一条完成了"，仍计入进度（留在 notes 数组里，不删）

[4] 在对话里输出进度行：`[已完成 X / 目标 N]`（X = notes.length，从 data.json 数）
    → 进度行输出后，**才能** back 回搜索页进入下一轮 LOOP
    → 进度行缺失 = 中间跳过了；出现连续两次相同的 X 必然意味着漏跑

当前快照里挑不到新笔记 → scroll 一次 → 回到 LOOP 入口 [1] 继续挑
（scroll 后旧 ref 全失效，反正本流程不缓存 ref，天然规避）

**终止条件只有两个，二选一，没有第三个：**
  (a) notes.length == target（抓够了）
  (b) 连续 scroll 到搜索结果**真的到底**（scroll 返回 atEnd:true，且到底后 inspect 仍无新卡片）
      —— 这时才允许少于 target，且必须走下面「没抓够怎么办」。

**"翻瀑布流找 note_id 太麻烦 / 太费 token" 不是终止条件，是必须继续做的工作。**
搜索页有几百条结果，滚 3-5 次找齐 5-10 条 note_id 是本任务的正常成本，不是意外开销。

全部跑完（达到 target 或真的到底）→ 补 data.json 顶层字段（kpi/lede/drive_dist/groups）
        → 跑 scripts/build-report.py 拼成 HTML 报告（见第 5 节），不许自己写 HTML
```

**顺序闸的逻辑：**

- 步骤 [1] → [2]：逼你"先看见、再落盘、再动手"。M3 最常见的偷懒是 inspect 后直接 click，标题都没留痕，事后根本没法核。
- 步骤 [2] → [3]：逼你"先把这条写进磁盘"。写进 data.json 的记录压缩冲不掉；如果点击失败，磁盘上就是真实失败记录；没落盘，回退到原搜索页快照就什么痕迹都没了。
- 步骤 [3] → [4]：逼你"先报数、再 back"。进度 X 从 data.json 数，只能累加不能跳，连续两次相同 X 就是漏跑信号。

**这条循环的设计目标是：让"跳过"在结构上不可能发生，** 而不是靠你自觉。
data.json 是进度的唯一真相来源，不是对话记忆，也不是 todo 列表。

### 每条都要报进度

第 4 步那句 `[已完成 X / 目标 N]` 是硬要求，不是可选的礼貌。

它的作用是让"跳过"在**过程中**就暴露：如果进度从 `[已完成 1 / 目标 10]`
直接跳到输出成品，用户一眼能看出中间 9 条压根没跑。

跑完最后一条，报一次收尾：

> 目标 <N> 条，已打开并验证 <X> 条，失败 <Y> 条（原因见对应条目）。

**进度只认两个东西：磁盘上的 `data.json` + 对话里的 `[已完成 X / 目标 N]` 行。**

**不许用 `todowrite` 记抓取进度。** 这是上一版被绕过的真实漏洞：模型只验证了 3 条，
却在 todo 里把"抓 5 条""补 5 条平替词"全标成了 `completed` —— todo 是"我打算做的事"，
不是"我做完的事"，把它当进度用，等于给自己发了张没兑现的完成证明。

- 抓取进度以 `data.json` 里 `notes` 数组的**实际条目数**（notes.length）为准
- todo 列表可以用来记"还要做关键词扩展、还要生成报告"这类**阶段**，但**单条笔记的完成与否不进 todo**
- 报进度就在对话里写 `[已完成 X / 目标 N]`，X 从 data.json 数出来，不从 todo 数

**ref 只能从 `inspect` 拿。** 这是全流程唯一的元素句柄来源：

- `inspect` = 拿全量快照（可见文本 + opaque ref）。本流程里每次只用它挑**下一条**要打开的笔记，
  用完即弃。它返回的 ref 只对**这一份快照**有效，`back` 回来后必须重新 `inspect`。
- `query` = **读内容，不给可点击 ref**。`kind:"text"` 读可见文本、`kind:"dom"` 读 DOM 片段。
  在本流程里它只有一个用途：第 0 节检查 B 里确认登录信号。
  不要拿它去"定位卡片"——拿不到能点的东西。

一句话：**每挑一条就 inspect 一次，不缓存 ref，不预先建清单。**

### 四条必须遵守的纪律

**① 登录态先确认。** 搜索页 `inspect` 后如果拿不到任何笔记卡片，
**先怀疑未登录，不要怀疑"这个词没内容"**——这是上面那个静默失败的表现。
确认办法：看页面是否出现登录引导 / 搜索框 placeholder 是否为「登录探索更多内容」。
未登录 → 停下告知用户去右侧浏览器登录，不要换词重试。

**② 滚动要克制，数量有真实天花板。** 搜索页是虚拟列表，`scroll` 之后视口上方卡片 DOM 被移除，
之前拿到的 opaque ref **全部失效**。

- 优先在"首屏 + 一次 scroll"能覆盖的范围内取满数量
- 数量不够时：**不要继续暴力滚**，改用搜索翻页，或换扩展词二次检索补齐
- 本流程不缓存 ref（每挑一条 inspect 一次），所以 scroll 导致的 ref 失效天然规避
- 反复滚超过 2-3 次基本是浪费 token，收益递减

**单次任务的真实容量：单关键词 10-15 条，全任务不超过 20 条。**

这不是保守设置，是算出来的：

| 环节 | 动作数 |
|---|---|
| 搜索页 navigate | 1 |
| 每条笔记：inspect 挑选 → click → inspect 详情 → back | 约 4 |
| 20 条 | 约 80 次浏览器动作 |

每次 `inspect` 都会返回一大段页面快照进上下文。20 条已经接近单次会话
能稳定完成的量；再往上走会先撑爆上下文，然后开始丢数据、错配、编数字——
**比少给几条糟得多**。

要更多条，用两种方式，别硬堆：
- 拆成多个关键词、多轮任务
- 用 `mavis cron` 每天跑一轮，攒出时间序列（顺便把"阶段"从推断变实测）

**③ 排序口径要说实话。** 我们拿到的是**搜索结果页综合排序**的内容，
然后自己按点赞数重排。真实口径是：

> 搜索结果页综合排序前 N 条中，按点赞数重排取前 X

不是"小红书点赞前 X"。交付时必须这么写。

细节展开见 `references/platform-notes.md`。

### ④ 为什么每条都必须真的打开

搜索页卡片上**只有标题、作者、点赞数**。收藏数、评论数、正文、标签、发布日期、note URL
全部只存在于笔记详情页。

**不打开详情页 = 拿不到 note URL，也拿不到藏/评两个数，第 4 节四项判断全部无从下手。**
那样交出去的不是选题清单，是一份带点赞数的标题列表。

已知会出现的偷懒路径，全部不允许：

- 只在搜索页 `inspect` 一次，把 10 条卡片整理成清单交付
- 打开 1-2 条，剩下的按搜索页卡片信息填表
- 因为"点开很花动作数"而跳过详情页
- 给未打开的条目标上 `搜索兜底`（那是失败标记，不是"没去做"的标记）

流程上的防呆已经在上面两段做了（**开抓前建骨架 + data.json 存档** + **单条闭环落记录**）。
按那个流程走，压根不会产生"待补全的卡片摘要"这种中间产物。

### 交付前兜底自检

正常情况下走完单条闭环，`✓已验证` 就等于实际完成条数，不需要这一步。
这是防线，不是常规路径，**只对"真的打不开"的情况**——不是给"懒得打开"留的减量口子
（跑不完要接着跑，见本节开头，不是砍 N）：

1. 数一遍 `✓已验证` 的条数
2. `✓已验证 / 目标条数 < 70%` → **不许直接出报告**。先输出：

> 目标 <N> 条，实际只验证到 <X> 条（其余详情页反复打不开）。
> 未打开的条目拿不到 note URL 和藏/评数据，报告的图表和判断列会残缺。
> 要我继续补齐剩下 <N-X> 条，还是就按这 <X> 条已验证的出报告？

3. 等用户回答，不要自己决定"先给个大概的"

**触发这条防线本身就说明流程跑歪了**——回头看是不是又变回"先建清单"了。

---

## 4. 选题判断（抓完不算完）

只给一个按点赞排的列表 = 交付了一份排行榜，用户还是不知道先做哪个。
每条追加四项判断：

- **阶段**：窗口开着 / 收窄 / 关了（发布时间 + 当前互动量推断）
- **驱动力**：信息差 / 实用 / 情绪 / 身份 / 猎奇（看赞藏评的**结构**，不只看总量）
- **门槛**：可直接做 / 需素材 / 需资源
- **相关性**：强相关 / 可延展 / 不相关

最后按可执行性分三组给结论：**现在就做 / 排期做 / 只做参考**。

### 没有数据就不许判断

四项判断各有其数据依赖，缺依据一律写 `未获取`，**不许看着标题猜**：

| 判断项 | 依赖 | 缺依据时 |
|---|---|---|
| 阶段 | 发布日期（详情页）+ 同题多条的时间分布 | `未获取` |
| 驱动力 | 赞 / 藏 / 评**三个数的比例**（藏和评只有详情页有） | `未获取` |
| 门槛 | 正文与配图形式（详情页） | `未获取` |
| 相关性 | 用户账号定位（第 1 节问到的）+ 笔记主题 | 可从标题判断，但要说明只依据标题 |

**只有点赞数就写不出驱动力。** 收藏>点赞是实用、评论>收藏是情绪/身份——
这些结论全靠比例。手上只有一个赞数还填"身份认同"，那是编的。

同理，单条卡片的"3天前"判不出窗口开没开，阶段要看**同题多条的发布时间分布**。

判断维度定义、阈值依据、互动结构怎么读 → `references/topic-judgment.md`。

---

## 5. 交付格式：可视化 HTML 报告

**本 skill 唯一的交付物是一份单文件 HTML 报告。你不写 HTML —— 你只填数据，然后跑拼装脚本。**

样式、三张图表、点击跳转逻辑**全部写死在 `assets/report-shell.html` 里**。
Chart.js 库已内联进 `assets/chart.umd.min.js`，所以报告**双击就能开、不依赖网络、图表能点击跳转**。

完整数据结构和填法见 `references/report-template.md`。核心就三步：

```
① 抓取时把进度存成 data.json（结构 = REPORT_DATA，逐条 append notes）
② 全部抓完补顶层字段（kpi/lede/drive_dist/groups），跑：
   python <skill目录>/scripts/build-report.py <data.json> <报告.html>
③ 用 <media type="file" src="<报告.html绝对路径>" /> 交付
```

### 为什么这样设计，不让模型写 HTML

上一版让模型自己写图表，它从零手写 SVG，调试几十轮还是渲染不出、没交互、不能点。
**根因是给了模型写 HTML 的自由。** 现在图表逻辑它看不到也不用碰——发挥空间为零，就不会翻车。

而且报告本身是**防偷懒的关卡**：

- KPI「最高藏赞比」：没有藏的数据算不出来
- 散点图：没有发布时间画不出点
- 柱状图：兜底条目的藏/评是 `null`，图上留**空缺口**，肉眼可见
- 驱动力环形图：缺数据的条目落进**灰色扇区**

数字必须来自真实抓取，**填错了图会画歪，填不满图有缺口**——偷懒会被图表公开展示。

### 链接硬约束（xsec_token）

每条 verified 笔记的 `url` 字段**必须带 `xsec_token`，且必须是从搜索页卡片 `href` 里原样读出来的。**

不带 token 的 `https://www.xiaohongshu.com/explore/<note_id>` 在浏览器里打不开——
实测跳 `/404`，`error_code=300031`、`error_msg=当前笔记暂时无法浏览`。
`xsec_token` 是平台下发的访问签名，**不能构造、不能猜、不能跨 note_id 复用**。

取法：`inspect` 搜索页 → 找 `href` 形如 `/search_result/<note_id>?xsec_token=...` 的 `a` 元素
→ 原样取 token → 拼成 `/explore/<note_id>?xsec_token=<token>&xsec_source=pc_search` 存进 `url`。

**图表的点击跳转就是打开这个 `url`。** token 错了，点击柱子会跳 404。报告页脚已写明链接可能失效，
不用你额外说明；但**不要**在别处承诺链接永久有效。

### 三级状态（写进每条的 status 字段）

| status | 含义 | 什么时候用 |
|---|---|---|
| `verified` | 详情页已打开，url + 藏/评拿到，标题作者与搜索页一致 | **正常状态，应占绝大多数** |
| `failed` | **试着打开过但失败了**（ref 反复失效 / 页面打不开），必须填 `fail_reason` | 异常兜底 |
| （丢弃） | 打开了但标题作者比对不上 —— 不写进 notes，但计入页脚抓取完整度说明 | 异常 |

**`failed` 是异常状态，不是省事的档位。**
"没去打开"不算 failed——那是漏做了单条闭环。只有**实际尝试并失败**才能标 `failed`，且要写明原因。

模板会自动处理呈现：`failed` 条卡片变灰、藏评显示"未获取"、图表里留缺口、页脚统计
"目标 N / 已验证 X / 兜底 Y"。你只要 status 和 fail_reason 填对，不用管样式。

事实与判断在报告里已自动分层（数字实线框 / 判断虚线框 / 建议橙左框）——
但前提是你把 `judge` 四项该留空的留空（缺数据写空，模板显示"未获取"），**不许猜**。

---

## 6. 可选：跑两轮拿增速

单次任务只能看到**当前互动量**，看不到增长速度，所以「阶段」只能标「推断」。

想变成实测，唯一办法是**同一组关键词隔 24 小时再抓一次做差**。
MiniMax Code 内置的 `mavis cron` 可以定时跑（`mavis({ command: "cron create", ... })`），
把两轮结果对比，涨得快的才是真在窗口里。

除此之外不做任何额外集成。生成的 HTML 报告直接交给用户，
要发到哪、接着做什么，都由用户在对话里另行安排——
本 skill 不假设你装了其他任何 skill。

---

## 7. 使用边界

这个 skill 只做一件事：**看公开内容，做人工可复核的记录。**

**做：**
- 只访问小红书公开可见的搜索结果与笔记页
- 用真实浏览器、正常人类操作节奏（单条抓取之间自然间隔）
- 记录公开互动数据与公开发布内容

**不做：**
- **不找达人 / 不找博主 / 不做选号**——本 skill 只抓"笔记内容"，账号维度不覆盖（见开头范围说明）
- 不含签名逆向代码（`x-s` / `x-t` / `x-s-common` 这类接口签名算法一律不实现）
- 不含 stealth 反检测注入、不伪造指纹
- 不构造直连 API 请求，只读浏览器已渲染出的可见内容
- 不批量刷量、不模拟点赞收藏关注、不做任何写操作
- 不采集非公开的个人信息（手机号、私信、位置轨迹等）
- 不做高频请求（每条之间留自然间隔，单次任务上限 20 条 —— 见第 3 节纪律②）
- 不做数据转售或 API 服务化

**遇到验证码 / 风控提示 → 立即停止，向用户报告，不尝试绕过。** 这条没有例外。

抓来的内容版权归原作者。做选题参考和公开引用可以，
直接搬运洗稿不行——这条不在技术层面拦你，是你自己的事。

平台服务条款以官方最新版本为准，使用者自行承担合规责任。
完整条款见仓库 `DISCLAIMER.md`。

---

## 附：踩坑速查

| 现象 | 原因 | 处理 |
|---|---|---|
| 搜索页一条笔记都拿不到，但页面正常 | **未登录**，服务端返 200 + `feeds: []` | 去右侧浏览器登录，别换词重试 |
| ref 报"元素不存在" | 虚拟列表滚动后卡片被卸载 | 重新 `inspect`，别复用旧 ref |
| 按位置序号取卡片总是取错 | 瀑布流各列高度不同，视觉位置 ≠ DOM 顺序 | 在 `inspect` 快照里按标题文本比对取 ref |
| 用 `query` 拿不到能点的元素 | `query` 只读文本/DOM，不返回 opaque ref | 拿 ref 一律用 `inspect` |
| `back` 之后动作全失败 | 列表重新渲染，ref 全失效 | `back` 后重新 `inspect` |
| 两次抓的互动数不一样 | 数据实时增长 | 标注抓取时点，别当永久事实 |
| 想按点赞排序但没有参数 | 搜索页只给综合排序 | 老实写"综合排序后重排" |
| 抓到一半开始丢数据 / 记错数字 | 上下文被 inspect 快照撑爆后压缩 | 进度落 `data.json`，压缩后读文件接着跑；单轮仍控制在 20 条内 |
| 报告里没有笔记链接 | 详情页没打开，只用了搜索页卡片 | 见第 3 节「单条闭环」：抓一条落一条 data.json，不预先建清单 |
| 只有点赞数，没有收藏/评论 | 同上，藏和评只有详情页有 | 补打开详情页，或标 `搜索兜底` + `未获取` |
| 驱动力/阶段填得很像但没数据支撑 | 看标题猜的 | 缺赞藏评比例一律写 `未获取`，不许推断 |
| 10 条里只有 1 条有链接，9 条标了兜底 | 先建了完整清单，逐条打开退化成"可选加料"被跳过 | 按单条闭环串行跑；进度以 data.json 为准 |
| todo 全标 completed 但实际只验证了几条 | 把 todo 当进度用，todo 是"打算做"不是"做完了" | 抓取进度只认 data.json + `[已完成 X / 目标 N]`，见第 3 节 |
| 图表里兜底条目的藏/评填了 0 | 0 表示"真没人收藏"，是撒谎 | 填 `null`，图上留空缺口才是"没抓到"的老实画法 |
| 用户其实想找博主/达人 | 目标物错位，本 skill 只抓笔记 | 直接说明不在范围内，别弹需求确认卡 |
| 要 10 条只抓了 5 条就停，说"剩下的要多滚/太费token" | 把"翻瀑布流麻烦"当成了终止条件 | 只有 scroll 到 atEnd 才算到底；没到底就接着滚，见第 3 节终止条件 |
| 兜底条目写不出失败原因 | 压根没尝试打开，不是打开失败 | `搜索兜底` 只限实际试过并失败；没试过就是漏做 |
| 交付的链接点开跳 404（`error_code=300031`） | 缺 `xsec_token`，链接是自己拼的 | token 只能从搜索页卡片 `href` 原样抄，见第 5 节 |
| 报告图表空白 / 自己手写了 SVG 图表 | 没用 build-report.py，自己写 HTML 了 | 只填 data.json 跑 `scripts/build-report.py`，图表逻辑已写死在模板，不许自己写 |
| 点击图表柱子跳 404 | 该条 `url` 少了 xsec_token | url 必须带原样 token，见第 5 节 |

---

## 来源

抓取流程、验证纪律、选题判断维度和踩坑清单来自真实任务实践。
第 0 节的未登录静默失败数据为 2026-07-29 实测。

许可证与免责声明见 `LICENSE` / `DISCLAIMER.md`。
